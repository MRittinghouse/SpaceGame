"""Centralized constants modules.

Contains shared names (dialogue_flag keys, event names, etc.) that cross
module boundaries. Any string contract that two modules agree on MUST
go through a module here, so a mismatch becomes impossible rather than
invisible. See ``requirements/stability_initiative.md``.
"""
